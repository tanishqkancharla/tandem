interface:
  display_name: "Generate Spec V2"
  short_description: "Write phased specs and serve them with tkstack"
  default_prompt: "Use $generate-spec-v2 to plan this feature as a phased implementation spec."
