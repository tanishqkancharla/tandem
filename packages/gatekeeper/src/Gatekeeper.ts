/**
 * Gatekeeper — a testing harness for intercepting service-to-service async calls.
 *
 * This is a placeholder export for the initial package scaffold.
 * Full builder, proxy, and invocation-handle implementation will follow in later phases.
 */

// eslint-disable-next-line @typescript-eslint/no-extraneous-class
export class Gatekeeper {}
